<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>Review:</strong>
  <%= @book.review %>
</p>
<p><%= image_tag @book.image.url(:large) %></p>

<% if user_signed_in? %><%= link_to 'Edit', edit_book_path(@book) %> |<% end %>
<%= link_to 'Back', books_path %>
